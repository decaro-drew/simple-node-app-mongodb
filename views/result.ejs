<!DOCTYPE html>
<body>
    <h1>Everything that has been added so far:</h1>
    <ul>
        <% for(var i = 0; i < result.length; i++){ %>
            <li><%=result[i].data%></li>
        <% } %>
    </ul>

</body>